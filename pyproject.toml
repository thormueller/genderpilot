[project]
name = "genderpilot"
version = "0.1.0"
description = "Cloudflare Python Worker for analyzing and improving gender-inclusive German texts."
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest",
    "workers-py",
    "workers-runtime-sdk",
]
